#include <stdio.h>

#define SIZE 3

int main(void) {
    char str[SIZE];
    str[0] = 'H';
    str[1] = 'i';
    str[2] = '\n';

    char str2[SIZE];
    str2[0] = '!';


    // if (strcmp(password, user_input) == 0) {
        // all the money
    // }
    // char str2[]
    printf("%s", str);
    return 0;
}